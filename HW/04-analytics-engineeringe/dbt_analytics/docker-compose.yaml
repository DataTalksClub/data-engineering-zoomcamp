version: '3'
services:
  dbt-bq-dtc:
    build:
      context: ../../../04-analytics-engineering/docker_setup
      target: dbt-bigquery
    image: dbt/bigquery
    volumes:
      - .:/usr/app
      - ~/.dbt/:/root/.dbt/
      - ~/.google/credentials/google_credentials.json:/.google/credentials/google_credentials.json
    network_mode: host